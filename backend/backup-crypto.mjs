import {createCipheriv,createDecipheriv,randomBytes} from 'node:crypto';
function key(value){if(!/^[a-f0-9]{64}$/i.test(value||''))throw new Error('BACKUP_ENCRYPTION_KEY doit contenir 64 caractères hexadécimaux');return Buffer.from(value,'hex')}
export function encodeBackup(data,secret){const iv=randomBytes(12),cipher=createCipheriv('aes-256-gcm',key(secret),iv);const content=Buffer.concat([cipher.update(JSON.stringify(data),'utf8'),cipher.final()]);return JSON.stringify({format:'hadhri-aes256gcm-v1',iv:iv.toString('base64'),tag:cipher.getAuthTag().toString('base64'),content:content.toString('base64')})}
export function decodeBackup(text,secret){const data=JSON.parse(text);if(data.format!=='hadhri-aes256gcm-v1')return data;const decipher=createDecipheriv('aes-256-gcm',key(secret),Buffer.from(data.iv,'base64'));decipher.setAuthTag(Buffer.from(data.tag,'base64'));return JSON.parse(Buffer.concat([decipher.update(Buffer.from(data.content,'base64')),decipher.final()]).toString('utf8'))}
