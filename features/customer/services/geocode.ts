export async function reverseGeocode(lat:number,lng:number):Promise<string>{const response=await fetch(`https://nominatim.openstreetmap.org/reverse?format=jsonv2&lat=${lat}&lon=${lng}&zoom=18&addressdetails=1`,{headers:{'Accept-Language':'fr'}});if(!response.ok)throw new Error('Adresse introuvable pour cette position.');const data=await response.json() as {display_name?:string};if(!data.display_name)throw new Error('Adresse introuvable pour cette position.');return data.display_name}
